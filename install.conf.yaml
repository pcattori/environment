- defaults:
    link:
      create: true
      relink: true

- clean:
  - '~'

- link:
    ~/.dotfiles: ''
    ~/.fzf: dependencies/fzf
    ~/.gitconfig:
    ~/.vimrc:
    ~/.zplug: dependencies/zplug
    ~/.zshrc:
    ~/.zsh:
