- defaults:
    link:
      create: true
      relink: true

- clean:
  - '~'

- link:
    ~/.bin:
    ~/.dotfiles: ''
    ~/.fzf: dependencies/fzf
    ~/.gitconfig:
    ~/.vimrc:
    ~/.zplug: dependencies/zplug
    ~/.zshrc:
    ~/.zsh:
